<Application x:Class="FormattinTextBoxBox.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FormattinTextBoxBox"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type local:FormattingTextBox}">
            <!--<Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="False">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type local:FormattingTextBox}">
                                <TextBox IsReadOnly="True" Text="{Binding FormattedText, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>-->
        </Style>
    </Application.Resources>
</Application>
